matrix:
  include:
    - os: linux
      language: node_js
      node_js:
        - "7"
    - os: osx
      language: node_js
      node_js:
        - "7"

branches:
  only:
  - master
  - develop

install:
  - npm install

script:
  - npm run dist